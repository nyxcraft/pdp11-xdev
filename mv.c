/* mv.c: not recognized as pdp11-cc (c0/c1) output.
 * No `jsr r5,csv'/`jmp cret' fingerprint -- this looks like
 * hand-written assembly, or Pascal/Fortran/other-compiler code.
 * Declining to decompile. */
